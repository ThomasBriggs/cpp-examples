#include "Vertex.h"
#include "Graph.h"
#include <iostream>
#include <unordered_map>

int main()
{
    // std::unordered_map<int, Vertex> map;
    // map.emplace(10, Vertex(10));
    // std::cout << (map.find(10) != map.end()) << '\n';
    // std::cout << map.find(10)->second.getData() << '\n';

    Graph g;
}

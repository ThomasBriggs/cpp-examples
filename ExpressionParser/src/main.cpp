#include <iostream>
#include "Expression.hpp"

int main(int argc, char const *argv[])
{
    // Expression a = Expression::parse("1 + 2");
    // std::cout << Expression::parse(argv[1]).eval() << std::endl;
    return 0;
}
